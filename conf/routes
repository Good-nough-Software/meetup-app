# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# An example controller showing a sample home page
GET        /                    controllers.LoginController.index

# Map static resources from the /public folder to the /assets URL path
GET        /assets/*file        controllers.Assets.versioned(path="/public", file: Asset)

# Controller for the User Account Settings
GET        /UserAccountSettings controllers.UserAccountSettingsController.UserAccountSettings

# Controller for the User Profile
GET        /UserProfile controllers.UserProfileController.UserProfile